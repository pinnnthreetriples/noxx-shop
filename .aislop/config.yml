# ponytail: score ratchet — CI baseline 80 today; raise as warnings get cleaned up
ci:
  failBelow: 75
rules:
  security/hardcoded-secret: error
  ai-slop/narrative-comment: error
  ai-slop/trivial-comment: error

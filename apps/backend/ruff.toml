# CI gate: syntax errors + undefined names only (full style pass not adopted yet)
lint.select = ["E9", "F63", "F7", "F82"]

[lint.per-file-ignores]
# SQLAlchemy string forward-references in Mapped["..."] are resolved at runtime
"**/models.py" = ["F821"]
